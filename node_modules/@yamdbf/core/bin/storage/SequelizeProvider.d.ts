import { StorageProviderConstructor } from '../types/StorageProviderConstructor';
export declare enum Dialect {
    Postgres = 0,
    SQLite = 1,
}
export declare function SequelizeProvider(url: string, dialect: Dialect): StorageProviderConstructor;
